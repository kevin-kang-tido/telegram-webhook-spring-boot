spring:
  datasource:
    url: jdbc:postgresql://localhost:1313/telegram_db
    username: telegram_spring
    password: telegram_spring123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

logging:
  level:
    root: INFO
